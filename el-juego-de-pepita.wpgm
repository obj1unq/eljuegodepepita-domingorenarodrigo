import wollok.game.*
import src.nido.*
import src.pepita.*
import src.silvestre.*

program ElJuegoDePepita {
  game.title("El juego de Pepita")
  game.height(12)
  game.width(12)
  game.boardGround("fondo.jpg")

  nido.inicializar()
  silvestre.inicializar()
  pepita.inicializar()
  game.start()
}